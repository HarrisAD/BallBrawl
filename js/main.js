// Main entry point for the game

// Wait for the page to load before initializing
window.addEventListener('load', function() {
    // Set up controls
    setupControls();
    
    // Initialize the game
    initGame();
    
    console.log('BallBrawl initialized successfully!');
});